{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/JWT_Console/JWT_Console.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}